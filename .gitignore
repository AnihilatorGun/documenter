.venv/
__pycache__/
*.pyc

# Secrets and machine-local state, never share through git:
# .env holds the Google client secret, data/local_state.json the Drive credential.
.env
data/
*.db
local_state.json
